import type { TemplateModule } from './types';

/** Stub when no site-local `./src` template is discovered. */
export const templates: TemplateModule[] = [];
export const template: TemplateModule | undefined = undefined;
export default null;
